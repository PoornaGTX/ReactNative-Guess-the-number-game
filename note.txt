elevation - styled property that used to make shadow

textAlign - align input area in a textinput but not align the textinput

maxLength - for text input

keyboardType -  this is used for select the keyborad type for text input (like number pad)

LinearGradient - is expo package is used to get gradient color

SafeAreaView - SafeAreaView ekak danne notch ekak thiya phone wala e notch ekata app eke content hiden wenne nethi wenna

Text - in a Text view we cant add border styles

useWindowDimensions() hook is executed with every phone orientation

KeyboardAvoidingView - is used when screen orientaion is landascape and avoid keyboard close not working this will use for ios device